import React, { useState } from "react";

function ChatView() {
  const [message, setMessage] = useState("");

  const handleSubmit = () => {
    // メッセージ送信処理（API呼び出し）
  };

  return <div>{/* チャット画面 */}</div>;
}

export default ChatView;
